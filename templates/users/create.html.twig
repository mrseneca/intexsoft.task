{% extends('base.html.twig') %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-sm-6">
                <h3>{{ title }}</h3>
                {{ form(form) }}
            </div>
        </div>
    </div>
{% endblock %}